<!DOCTYPE html>
<html lang="et">
<head>
    <meta charset="UTF-8">
    <title>Kevin Lillemets, veebiprogrammeerimine</title>
</head>
<body>
    <img src="/images/AA.png" alt="Kursuse bänner">
    <h1>Kevin Lillemets, veebiprogrammeerimine</h1>
    <p>See leht on loodud <a href="https://www.tlu.ee/">Tallinna Ülikoolis</a> veebiprogrammeerimise kursusel ja ei oma mõistlikku sisu.</p>
    <hr>
	<ul>
		<li><a href="/">Avalehele</a></li>
		<li><a href="/news">Uudiste avalehele</a></li>
	</ul>
	<h2>Aegumata uudised</h2>
	<% if (locals.newsData && newsData.length > 0) { %>
		<% for(let i = 0; i < newsData.length; i++){ %>
			<h3><%= newsData[i].title %></h3>
			<p>Lisatud: <%= newsData[i].added.toLocaleString("et-EE") %></p>
			<% if (newsData[i].filename) { %>
				<img src="/newsphotos/<%= newsData[i].filename %>" alt="<%= newsData[i].alttext %>" style="max-width: 400px; height: auto;">
				<br>
			<% } %>
			<p><%= newsData[i].content %></p>
			<hr>
		<% } %>
	<% } else { %>
		<p>Aegumata uudiseid hetkel ei ole.</p>
	<% } %>
</body>
</html>