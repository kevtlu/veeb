<!DOCTYPE html>
<html lang="et">
<head>
    <meta charset="UTF-8">
    <title>Kevin Lillemets, veebiprogrammeerimine</title>
</head>
<body>
    <img src="/images/AA.png" alt="Kursuse bänner">
    <h1>Kevin Lillemets, veebiprogrammeerimine</h1>
    <p>See leht on loodud <a href="https://www.tlu.ee/">Tallinna Ülikoolis</a> veebiprogrammeerimise kursusel ja ei oma mõistlikku sisu.</p>
    <hr>
	<ul>
		<li><a href="/">Avalehele</a></li>
		<li><a href="/eestifilm/">Eesti filmi avaleht</a></li>
		<li><a href="/eestifilm/seosed">Seosed isikute, filmide ja ametite vahel</a></li>
	</ul>
	<h2>Seoste lisamine</h2>
	<form method="POST">
        <div>
            <label for="personSelect">Isik:</label>
            <select id="personSelect" name="personSelect" required>
                <option selected disabled value="">Vali isik</option>
                <% personList.forEach(person => { %>
                    <option value="<%= person.id %>">
                        <%= person.first_name %> <%= person.last_name %>
                    </option>
                <% }) %>
            </select>
        </div>

        <div>
            <label for="filmSelect">Film:</label>
            <select id="filmSelect" name="filmSelect" required>
                <option selected disabled value="">Vali film</option>
                <% filmList.forEach(film => { %>
                    <option value="<%= film.id %>"><%= film.title %></option>
                <% }) %>
            </select>
        </div>

        <div>
            <label for="positionSelect">Amet:</label>
            <select id="positionSelect" name="positionSelect" required>
                <option selected disabled value="">Vali amet</option>
                <% positionList.forEach(position => { %>
                    <option value="<%= position.id %>"><%= position.position_name %></option>
                <% }) %>
            </select>
        </div>       
        <br>
        <input id="relationSubmit" type="submit" name="relationSubmit" value="Salvesta seos">
    </form>
</body>
</html>